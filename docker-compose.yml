version: '3'
services:

  postgres:
    image: "postgres:13.3"
    ports:
      - "5433:5433"
    environment:
      POSTGRES_USER: "postgres"
      POSTGRES_PASSWORD: "dockerpass"
      POSTGRES_DB: "MyFriends"

